import CoreGraphics
enum Strings {
    static let navigationControllerTitle = "Настройки"
}

enum Metric {
    static let cellIconViewCornerRadius: CGFloat = 5
    static let iconeViewSize: CGFloat = 30
    static let labelLeftOffset: CGFloat = 10
}
